apiVersion: v1
kind: Service
metadata:
  name: {{ include "essential-invoice.backend.serviceName" . }}
  labels:
    {{- include "essential-invoice.backend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "essential-invoice.backend.selectorLabels" . | nindent 4 }}
